# tonk notification personality — https://github.com/zandoh/tonk
# username unset on purpose: posts use the tonk brand default (name + icon
# as of tonk v2.1.0).
notify:
  failure: true
  recovery: true
  pull-requests: false # red PRs are part of iterating
